import React from 'react'

class Admin extends React.Component {
    constructor(props) {
        super(props)
    }

    render() {
        const { path } = this.props
        let component;
        switch(path) {
            case "recipe_edit":
            
            case "recipes":

            default:
        }
    }
}

export default Admin