from filterLibrary import * 


class recipeParser: 

	def __init__(self, file_path): 
		self.file_path = file_path

	def parse_ingredients(self): 
		self.ingredients = []
		#Start at line that says Ingredients 
		#filter all ingredients 
		# new_ingred = filter_ingred()
		# ingredients.append(new_ingred)
		#End at line that says "Instructions"

		#pickle
		#nametuple

def main(): 
	parse_ingredients

if __name__=="__main__": 
	main()